# EmergencyRND V2 — Туториал

## Overview
Пошаговый интерактивный туториал для новых игроков. Запускается **только при выборе уровня 1**. Проводит игрока через все базовые механики: открытие смены, работа с пациентами, магазин, лечение, касса, смена белья, стирка. Все действия, не относящиеся к текущему шагу, **заблокированы** визуально и технически.

## Файл: `js/tutorial.js`

### Архитектура
IIFE-модуль `Game.Tutorial` — конечный автомат (state machine) с массивом шагов `STEPS[]`. Каждый шаг определяет:
- `text` — текст инструкции (русский)
- `trigger` — событие для перехода к следующему шагу (`'next_button'` / `'shift_opened'` / `'popup_opened'` и т.д.)
- `allowed[]` — список разрешённых действий (всё остальное заблокировано)
- `pauseTime` — останавливать ли игровое время
- `position` — позиция панели (`'center'`, `'left'`, `'top-right'`)
- `spotlight` — CSS-селектор элемента для подсветки (spotlight)
- `needsPointerLock` — нужен ли захват мыши для 3D-взаимодействия
- `arrow3D` — координаты для 3D-стрелки `{x, y, z}`
- `arrowTrackPatient` — стрелка следит за пациентом
- `arrowTrackBed` — стрелка следит за грязной кроватью
- `arrowOffsetY` — высота стрелки над целью
- `onEnter` / `onExit` — коллбэки при входе/выходе из шага
- `isFinal` — последний шаг (кнопка "Начать!")

### Публичный API
```js
Game.Tutorial.setup(controls, THREE, camera, scene)  // инициализация
Game.Tutorial.checkStart()                            // проверка и запуск (вызывается из levels.js)
Game.Tutorial.isActive()                              // активен ли туториал
Game.Tutorial.isPaused()                              // нужно ли паузить игровое время
Game.Tutorial.isAllowed(actionId)                     // разрешено ли действие на текущем шаге
Game.Tutorial.onEvent(eventName, data)                // модули вызывают при событиях
Game.Tutorial.setTutorialPatient(patient)              // сохраняет ссылку на первого пациента
Game.Tutorial.update(delta)                           // обновление 3D-стрелки (вызывается в animation loop)
```

### Активация
- Вызывается из `levels.js` при выборе уровня 1: `Game.Tutorial.checkStart()`
- Запускается после первого `controls.lock` (когда игрок кликнул overlay)
- **Не сохраняется** в localStorage — запускается при каждом выборе уровня 1

### Блокировка действий
Каждый модуль проверяет `Game.Tutorial.isAllowed(actionId)` перед выполнением действия. Если туториал не активен, `isAllowed()` всегда возвращает `true`.

**Action IDs:**
- `movement`, `camera` — перемещение и камера
- `sign_click` — клик по табличке смены
- `patient_click` — клик по пациенту
- `treat_patient` — применение препарата на пациента
- `btn_bed`, `btn_wait`, `btn_dismiss` — кнопки попапа пациента
- `shop_open`, `shop_close`, `shop_buy` — магазин
- `shelf_take`, `shelf_place` — взятие/размещение на стеллаже
- `pickup_item`, `pickup_box`, `drop_item` — подбор/бросок предметов
- `cashier_click` — клик по терминалу кассы
- `linen_replace` — замена белья на кровати
- `washing_machine` — загрузка/запуск стиральной машины
- `furniture_interact` — перемещение мебели
- `trash_interact` — уборка мусора

## Последовательность шагов (25 шагов, индексы 0–24)

### Открытие смены
| # | Описание | Триггер | Разрешено | Стрелка |
|---|----------|---------|-----------|---------|
| 0 | Приветствие | Далее | — | — |
| 1 | Открыть смену (ЛКМ на табличке) | `shift_opened` | movement, sign_click | Табличка (2.5, 1.2, -0.08) |
| 2 | Подойти к пациенту (ЛКМ) | `popup_opened` | movement, patient_click | Следит за пациентом |

### Работа с пациентом
| # | Описание | Триггер | Разрешено | Spotlight |
|---|----------|---------|-----------|----------|
| 3 | Объяснение попапа пациента | Далее | — | `#patient-popup` |
| 4 | Нажать "На кровать" | `patient_sent_to_bed` | btn_bed | `#btn-bed` |

### Магазин и покупка препарата
| # | Описание | Триггер | Разрешено |
|---|----------|---------|-----------|
| 5 | Открыть магазин (Q) — с цветовой подсказкой препарата | `shop_opened` | movement, shop_open |
| 6 | Купить нужный препарат (остальные кнопки заблокированы) | `shop_item_bought` | shop_buy, shop_close |
| 7 | Закрыть магазин | `shop_closed` | shop_close |

### Лечение
| # | Описание | Триггер | Стрелка |
|---|----------|---------|---------|
| 8 | Взять препарат из коробки (ЛКМ) | `item_picked_up` | Зона доставки (0, 0.5, 5) |
| 9 | Применить на пациента (ЛКМ) | `patient_treated` | Следит за пациентом |
| 10 | Инфо: пациент выздоравливает | Далее | — |

### Касса
| # | Описание | Триггер | Стрелка |
|---|----------|---------|---------|
| 11 | Подойти к кассе (ЛКМ) | `terminal_opened` | Касса (3.5, 0.5, -9.5) |
| 12 | Ввести сумму, нажать OK | `payment_done` | Spotlight `#cashier-popup` |

### Смена белья
| # | Описание | Триггер | Стрелка |
|---|----------|---------|---------|
| 13 | Инфо: кровать стала грязной | Далее | — |
| 14 | Открыть магазин для белья | `shop_opened` | — |
| 15 | Купить постельное бельё | `shop_item_bought` | Spotlight `#shop-tab-consumables` |
| 16 | Закрыть магазин | `shop_closed` | Spotlight `#shop-popup` |
| 17 | Взять бельё из коробки | `item_picked_up` | Зона доставки (0, 0.5, 5) |
| 18 | Заменить бельё (ЛКМ на грязной кровати) | `linen_replaced` | Следит за грязной кроватью |

### Стирка
| # | Описание | Триггер | Стрелка |
|---|----------|---------|---------|
| 19 | Инфо: грязное бельё можно постирать | Далее | — |
| 20 | Загрузить грязное бельё в машинку (ЛКМ) | `linen_loaded` | Машинка (5.5, 0.8, -10.5) |
| 21 | Запустить стирку (E) | `wash_started` | Машинка |
| 22 | Ждать 20 секунд | `wash_finished` | Машинка |
| 23 | Подобрать чистое бельё | `item_picked_up` | Машинка (5.5, 1.2, -10.5) |

### Завершение
| # | Описание | Триггер |
|---|----------|---------|
| 24 | Готово! | Начать! |

## UI элементы

### HTML (`index.html`)
```html
<div id="tutorial-overlay">
  <div id="tutorial-spotlight"></div>
  <div id="tutorial-panel">
    <div id="tutorial-step-num"></div>
    <div id="tutorial-text"></div>
    <button id="tutorial-next">Далее</button>
  </div>
</div>
```

### CSS (z-index)
- `#tutorial-spotlight` — z-index: 16 (ниже попапов z:20, выше HUD z:5)
- `#tutorial-panel` — z-index: 30 (выше всех попапов)
- `#tutorial-overlay` — z-index: 28

### Spotlight
Реализован через `box-shadow: 0 0 0 9999px rgba(0,0,0,0.6)` — создаёт тёмный оверлей с прозрачной "дыркой" вокруг целевого элемента.

### Заблокированные кнопки
CSS-класс `.tutorial-locked`:
```css
.tutorial-locked {
  opacity: 0.35 !important;
  pointer-events: none !important;
  filter: grayscale(0.8) !important;
}
```

## 3D-стрелка
Голубая 3D-стрелка (цилиндр + конус + кольцо) из `THREE.MeshBasicMaterial({ color: 0x44bbff })`:
- Парит над целевой точкой с покачиванием (sin-анимация)
- Медленно вращается вокруг оси Y
- Поддерживает статические координаты (`arrow3D`) и динамическое слежение (`arrowTrackPatient`, `arrowTrackBed`)

## Первый бесплатный заказ
В `shop.js` реализована механика: первый заказ каждого типа расходников — бесплатный.
- Словарь `firstOrderUsed{}` — отслеживает использованные бесплатные заказы
- Кнопки показывают "Заказать — Бесплатно!" зелёным цветом
- После использования: обычная цена "Купить — $80/$100"

## Стеллажи
Стеллажи стартуют **пустыми** — игрок покупает расходники через магазин. Предзаполнение удалено.

## Интеграция с модулями

### События, отправляемые модулями:
| Модуль | Событие | Когда |
|--------|---------|-------|
| `shift.js` | `shift_opened` | После `openShift()` |
| `patients.js` | `popup_opened` | После открытия попапа пациента |
| `patients.js` | `patient_sent_to_bed` | После нажатия "На кровать" |
| `patients.js` | `patient_treated` | После применения препарата |
| `shop.js` | `shop_opened` / `shop_closed` | При открытии/закрытии магазина |
| `shop.js` | `shop_item_bought` | После покупки (с типом предмета) |
| `cashier.js` | `terminal_opened` | При открытии терминала |
| `cashier.js` | `payment_done` | После успешной оплаты |
| `shelves.js` | `item_taken_from_shelf` | При взятии предмета (с типом) |
| `furniture.js` | `linen_replaced` | После замены белья |
| `washing-machine.js` | `linen_loaded` | После загрузки белья |
| `washing-machine.js` | `wash_started` | После запуска стирки |
| `washing-machine.js` | `wash_finished` | После завершения стирки |
| `consumables.js` | `item_picked_up` | При подборе предмета (из коробки или с земли) |

### Блокировки в модулях:
| Модуль | Что блокируется |
|--------|----------------|
| `shift.js` | Клик по табличке, пауза времени, блокировка спавна |
| `patients.js` | Клик по пациенту, кнопки попапа, HP-decay, спавн по таймеру и после оплаты |
| `shop.js` | Открытие/закрытие, покупки |
| `cashier.js` | Клик по терминалу, кнопка закрытия скрыта во время туториала |
| `shelves.js` | Взятие и размещение предметов, уступает кассе при конфликте рейкастов |
| `consumables.js` | Подбор, бросок, E-подъём коробок |
| `furniture.js` | Перемещение мебели, замена белья |
| `washing-machine.js` | Загрузка и запуск (LMB и E) |
| `trash.js` | Уборка мусора |
| `controls.js` | Overlay не показывается при активном туториале; re-lock при ESC |

### Порядок в animation loop:
```
Game.Cashier.update(delta)   // до shelves — кассир имеет приоритет
Game.Shelves.update(delta)   // уступает кассиру
Game.Tutorial.update(delta)  // обновление 3D-стрелки
```

### Завершение туториала:
При вызове `finish()`:
1. `active = false` — все блокировки снимаются
2. Убираются UI (overlay, spotlight, стрелка)
3. Разблокируются все кнопки и табы
4. `controls.lock()` — возврат к управлению
5. `Game.Patients.spawnFirstPatient()` — спавн первого пациента после туториала
